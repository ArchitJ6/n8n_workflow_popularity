version: '3.8'

services:
  n8n-popularity-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
      - DEVELOPMENT_MODE=${DEVELOPMENT_MODE}
    volumes:
      - ./data:/app/data
    restart: unless-stopped